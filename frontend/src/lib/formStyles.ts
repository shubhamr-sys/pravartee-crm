/** Shared classes for inputs/selects — always light bg + dark text (browser dark mode safe). */
export const formFieldClass =
  "w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm text-slate-900 outline-none focus:border-teal-600 focus:ring-2 focus:ring-teal-100";

export const formFieldClassLg = `${formFieldClass} py-2.5`;

export const formLabelClass = "mb-1.5 block text-sm font-medium text-slate-700";
